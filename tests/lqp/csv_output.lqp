(transaction
  (epoch
    (writes
      (define
        (fragment :frag
          (def :test
            ([x::INT]
              (+ 1 1 x))))))

    (reads
      (output_export
        (csv
          (table_def :test)
          (csv_config {}))))))
