(transaction
  (epoch
    (writes
      (define
        (fragment :frag
          (def :test
            ([x::INT]
              (+ 1 1 x))))))

    (reads
      (export_output :my_output
        (csv
          (table_def :test)
          (csv_config {}))))))
