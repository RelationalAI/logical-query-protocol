(transaction
  (configure { :ivm.maintenance_level "all" :semantics_version 2})
  (epoch
    (writes (define (fragment :f2 (def 0xdb77fd01af957221 ([] (= 2 2))))))
    (reads (output :r2 0xdb77fd01af957221))))

;; Debug information
;; -----------------------
;; Original names
;; 	 ID `0xdb77fd01af957221` -> `r2`
