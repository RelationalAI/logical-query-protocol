(transaction
    (epoch
        (local_writes
            (define
                (fragment :f1
                    (def :foo ([x::INT] (relatom (sig :hello/INT) x::INT)))
                )
            )
        )

        (reads
            (output :foo :foo)
        )
    )
)
