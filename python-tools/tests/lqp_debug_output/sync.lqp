(transaction
  (configure
    { :ivm.maintenance_level "off"
      :semantics_version 0})
  (sync :foo :bar)
  (epoch
    (writes
      (define
        (fragment :frag
          (def 11495104353665842533
            ([x::INT y::INT]
              (not
                (primitive :rel_primitive_eq x y)))))))))

;; Debug information
;; -----------------------
;; Original names
;; 	 ID `11495104353665842533` -> `test`
