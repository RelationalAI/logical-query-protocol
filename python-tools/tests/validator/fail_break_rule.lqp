;; ERROR: Break rule found outside of body at fail_break_rule.lqp:10:17: '3181428560199927439'
(transaction
  (epoch
    (local_writes
      (define (fragment :f1
        (algorithm :foo
          (script
            (loop
              (init
                (break :foo ([] (or))))
              (script
                (assign :foo ([] (or))))))))))))
