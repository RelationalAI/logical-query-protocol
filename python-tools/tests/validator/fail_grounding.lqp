;; ERROR: Variables not grounded: y
(transaction
  (epoch
    (local_writes
      (define
        (fragment :frag
          (def :bad
            ([x::INT y::INT]
              (or
                (atom :foo x y)
                (atom :bar x)))))))))
