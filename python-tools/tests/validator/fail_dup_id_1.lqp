;; ERROR: Duplicate declaration within fragment in epoch at fail_dup_id_1.lqp:10:11: '14069321933851954459'
;; Duplicate ID, same fragment, same epoch.
(transaction
  (epoch
    (local_writes
      (define
        (fragment :frag
          (def :cool
            ([] (and)))
          (def :cool
            ([] (or))))))
    (reads)))
