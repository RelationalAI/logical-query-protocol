;; ERROR: All data columns in ExportCSVConfig at fail_csv_config_2.lqp:20:9 must have the same key types. Got 'col1' with key types ['STRING'] and 'col2' with key types ['INT'].
(transaction
  (epoch
    (writes
      (define
        (fragment :f1
          (def :col1
            ([row::STRING v::INT]
              (and
                (= row "foo")
                (= 2 v))))
          (def :col2
            ([row::INT v::STRING]
              (and
                (= row 1)
                (= v "hello")))))))

    (reads
      (export
        (export_csv_config
          (path "wrong")
          (columns (column "col1name" :col1) (column "col2name" :col2))
          { :partition_size 10
            :compression ""
            :syntax_quotechar "\""
            :syntax_escapechar "\\"})))))
