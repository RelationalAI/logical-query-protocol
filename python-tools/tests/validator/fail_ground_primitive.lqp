;; ERROR: Variables not grounded: x
(transaction
  (epoch
    (local_writes
      (define
        (fragment :frag
          (def :bad
            ([x::INT y::INT]
              (and
                (atom :foo y)
                (primitive :plus x y)))))))))
