;; No problem because the "duplication" happens in a separate epoch.
(transaction
  (epoch
    (writes
      (define
        (fragment :frag
          (def :cool
            ([] (or)))))
    )
    (reads))
  (epoch
    (writes
      (define (fragment :frag))
    )))
