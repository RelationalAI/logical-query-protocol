;; ERROR: Duplicate declaration across fragments at fail_dup_id_2.lqp:7:29: '3181428560199927439'
;; Duplicate ID, different fragment, same epoch.
(transaction
  (epoch
    (local_writes
      (define (fragment :f1 (def :foo ([] (or)))))
      (define (fragment :f2 (def :foo ([] (or))))))
    (reads)))
