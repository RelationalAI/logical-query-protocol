;; ERROR: Duplicate declaration at fail_dup_id_2.lqp:6:29: '3181428560199927439'
(transaction
  (epoch
    (local_writes
      (define (fragment :f1 (def :foo ([] (or)))))
      (define (fragment :f2 (def :foo ([] (or))))))
    (reads)))
