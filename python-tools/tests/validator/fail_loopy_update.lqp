;; ERROR: Upsert at fail_loopy_update.lqp:9:15 must have an Atom as its value
(transaction
  (epoch
    (writes
      (define
        (fragment :copy
          (algorithm :test1
            (script
              (upsert::1 :test1 ([] (and ))))))))))