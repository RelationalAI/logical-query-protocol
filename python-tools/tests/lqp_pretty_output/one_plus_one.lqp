(transaction
  (epoch
    (local_writes
      (define
        (fragment :f1
          (def :output
            ([v::INT]
              (primitive :rel_primitive_add_monotype 1 1 v))
            ))))
    (reads
      (output :output :output))))