(transaction
  (epoch
    (writes
      (define
        (fragment :f1
          (def :output
            ([v::INT]
              (primitive :rel_primitive_add_monotype 1::INT 1::INT v))))))
    (reads
      (output :output :output))))
