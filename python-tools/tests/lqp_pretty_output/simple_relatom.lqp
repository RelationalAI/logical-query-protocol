(transaction
  (epoch
    (writes
      (define
        (fragment :f1
          (def :foo
            ([x::INT]
              (relatom :hello x)))
          (def :bar
            ([x::INT y::STRING]
              (relatom :bar #"sp_str"::STRING x #123::INT y))))))
    (reads
      (output :foo :foo)
      (output :bar :bar))))
