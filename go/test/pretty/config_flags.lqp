(transaction
  (configure
    { :ivm.maintenance_level "auto"
      :semantics_version 1})
  (epoch
    (writes
      (define
        (fragment :f6631
          (def :output
            ([v::INT]
              (primitive :rel_primitive_add_monotype 1 1 v))))))
    (reads
      (output :output :output))))
