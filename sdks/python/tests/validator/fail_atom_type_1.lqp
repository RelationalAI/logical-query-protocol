;; ERROR: fail_atom_type_1.lqp:8:25: Incorrect type for 'foo' atom at index 1 ('2'): expected STRING term, got INT
(transaction
  (epoch
    (writes
      (define
        (fragment :f1
          (def :foo ([x::STRING y::STRING] (and (= x "A") (= y "B"))))
          (def :bar ([] (atom :foo "A" 2))))))
    (reads)))
