;; ERROR: fail_dup_id_1.lqp:10:11: Duplicate declaration within fragment in epoch: 'cool'
;; Duplicate ID, same fragment, same epoch.
(transaction
  (epoch
    (writes
      (define
        (fragment :frag
          (def :cool
            ([] (and)))
          (def :cool
            ([] (or))))))
    (reads)))
