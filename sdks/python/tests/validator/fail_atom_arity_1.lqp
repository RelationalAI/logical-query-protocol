;; ERROR: Incorrect arity for 'foo' atom at fail_atom_arity_1.lqp:8:25: expected 0 terms, got 1
(transaction
  (epoch
    (writes
      (define
        (fragment :f1
          (def :foo ([] (or)))
          (def :bar ([] (atom :foo 1))))))
    (reads)))
