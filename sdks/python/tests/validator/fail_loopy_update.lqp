;; ERROR: fail_loopy_update.lqp:9:15: Upsert must have an Atom as its value
(transaction
  (epoch
    (writes
      (define
        (fragment :copy
          (algorithm :test1
            (script
              (upsert :test1 ([] (and ))))))))))